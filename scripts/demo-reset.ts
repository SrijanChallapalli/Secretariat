/**
 * Optional: reset demo state (e.g. burn or revoke). For MVP we only log instructions.
 */
console.log("Demo reset: use a fresh deploy or same contracts with new wallets.");
console.log("No on-chain reset implemented for MVP; redeploy contracts for clean state.");
