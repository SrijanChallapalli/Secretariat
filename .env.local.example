# ===========================================================================
#  Secretariat — Local Testing (no testnet credentials)
#  Copy to .env:  cp .env.local.example .env
#  Then run:  npm run test:local
# ===========================================================================
#  This runs: Anvil + deploy + seed + dev. All features work locally.
# ===========================================================================

# Anvil default account (NEVER use in production)
DEPLOYER_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Local RPC
RPC_URL=http://127.0.0.1:8545
RPC_0G=http://127.0.0.1:8545
CHAIN_ID_0G=31337

# Enable mock 0G storage (no OG_UPLOADER_PRIVATE_KEY needed)
LOCAL_TESTING=true

# App uses Anvil local as initial chain
NEXT_PUBLIC_CHAIN_ID=31337

# Demo heatmap: fake valuation change % for heatmap tiles (no oracle feed yet)
# NEXT_PUBLIC_DEMO_HEATMAP=true

# WalletConnect (optional for local — use 32 zeros if no project)
NEXT_PUBLIC_WALLETCONNECT_ID=00000000000000000000000000000000

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SERVER_URL=http://localhost:4000

# Contract addresses — filled by: npm run deploy:local && npm run env:from-broadcast 31337
NEXT_PUBLIC_ADI_TOKEN=
NEXT_PUBLIC_HORSE_INFT=
NEXT_PUBLIC_BREEDING_MARKETPLACE=
NEXT_PUBLIC_SYNDICATE_VAULT=
NEXT_PUBLIC_HORSE_ORACLE=
NEXT_PUBLIC_SYNDICATE_VAULT_FACTORY=
NEXT_PUBLIC_AGENT_INFT=
NEXT_PUBLIC_AGENT_EXECUTOR=
